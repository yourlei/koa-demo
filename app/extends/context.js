"use strict"
/**
 * 自定义App#context对象的扩展方法
 */
module.exports = App => {
    /**
     * @name validate
     * @desc 参数验证器
     * @param {object} rules 验证规则
     * @param {object} target 验证的对象
     * 
     * @return Object
     */
    validate: (rules, target) => {
        
    }
}